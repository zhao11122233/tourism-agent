external_data_path: data/external/records.csv
pricing_data_path: data/pricing/scenic_spots.json

# 票务客户端配置
ticket_client:
  base_url: ""        # 预留，为空时使用内置模拟数据
  timeout: 10         # 请求超时（秒）
  retry: 2            # 失败重试次数
